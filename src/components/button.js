import tw from 'tailwind.macro';

const Button = tw.button`
bg-white font-bold rounded-full mt-6 py-4 px-8 shadow-lg uppercase tracking-wider bg-transparent hover:bg-blue-500 text-blue-700 hover:text-white
`;

export default Button;
